<Window x:Class="Zadatak07.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525">
    <Window.Resources>
        <Style x:Key="stil" TargetType="{x:Type Control}">
            <Setter Property="Background" Value="Purple"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="Width" Value="50"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <RotateTransform Angle="30"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <StackPanel Orientation="Horizontal">
        <Button Content="Gumb 1" Style="{StaticResource ResourceKey=stil}"/>
        <Button Content="Gumb 2" Style="{StaticResource ResourceKey=stil}"/>
        <Button Content="Gumb 3" Style="{StaticResource ResourceKey=stil}"/>
    </StackPanel>
</Window>
